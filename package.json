{
  "name": "menuDo",
  "version": "0.0.5",
  "description": "A wrapper for todo.microsoft.com",
  "dependencies": {
    "menubar": "^5.2.3",
    "npm": "^6.4.1"
  },
  "devDependencies": {
    "electron-packager": "^5.1.0",
    "electron-prebuilt": "^0.36.0"
  },
  "main": "main.js",
  "scripts": {
    "build": "electron-packager . MenuDo --platform=darwin --arch=x64 --version=0.36.0 --icon=Icon.icns",
    "start": "electron ."
  }
}
